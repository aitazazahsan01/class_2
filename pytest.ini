[pytest]
; Only collect test_* functions, not classes -- avoids pytest mistaking the
; TestCase/TestResult dataclasses (named that way for domain clarity, not as
; pytest fixtures) for test classes.
python_classes =
python_files = test_*.py
python_functions = test_*
